import SwiftUI


struct ASMediaViewPlaceholderView: View {
    var body: some View {
        VStack {
            Text("No content.")
                .foregroundColor(.secondary)
        }
        .padding()
    }
}
